# UI / デザインルール
- UIコンポーネントは独自実装せず、基本的に `shadcn/ui` を使用すること
- 必要に応じて `npx shadcn@latest add <component>` を実行してコンポーネントを追加すること
- アニメーションやリッチな視覚効果が必要な場合は `Aceternity UI` または `Magic UI` を採用すること
- 配色は安易な紫系グラデーションを避け、ニュートラルで洗練されたモダンUIにすること
